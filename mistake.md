1.python-jose install mistake
2. what will i return from jwt decode i return User not just user_id
3. then in post update the user_id by this current user id not it will automatically assign by foreign key
4. in filter without bracket use & operations